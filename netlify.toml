[build]
  command = "pnpm install --frozen-lockfile && cd apps/web-client && npx vite build --mode demo"
  publish = "apps/web-client/dist"

[build.environment]
  VITE_DEMO_MODE = "true"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
