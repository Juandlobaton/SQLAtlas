[build]
  command = "npx vite build --mode demo"
  publish = "dist"

[build.environment]
  VITE_DEMO_MODE = "true"
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
